# --- Module 1: Core setup ---
python-dotenv==1.0.1
pydantic-settings==2.4.0
loguru==0.7.2
streamlit==1.38.0
pytest==8.3.2

# --- Module 3: Document Loading ---
pypdf==4.3.1
python-docx==1.1

# --- Module 4: Text Chunking ---
langchain-text-splitters==0.3.0

# --- Module 5: Embeddings ---
sentence-transformers==3.0.1

# --- Module 6: Vector Database ---
chromadb==0.5.5
# NOTE: LangChain, ChromaDB, sentence-transformers, ollama, pypdf,
# python-docx, etc. will be appended in the modules that introduce them.
# Keeping requirements.txt scoped-per-module avoids dependency bloat
# and keeps install times fast during early development.